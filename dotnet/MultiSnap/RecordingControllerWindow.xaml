<Window x:Class="MultiSnap.RecordingControllerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recording" Height="74" Width="286"
        WindowStyle="None" ResizeMode="NoResize" Topmost="True" ShowInTaskbar="False"
        Background="Transparent" AllowsTransparency="True" WindowStartupLocation="Manual">
    <Border Background="#E610141C" BorderBrush="#526074" BorderThickness="1" CornerRadius="8" Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Recording" Foreground="{StaticResource TextPrimaryBrush}"
                           FontWeight="SemiBold" />
                <TextBlock x:Name="TimerText" Text="00:00" Foreground="{StaticResource AccentWarmBrush}"
                           FontSize="18" FontWeight="SemiBold" Margin="0,2,0,0" />
            </StackPanel>

            <Button Grid.Column="1" Content="Stop" Width="66" Height="34" Margin="10,0,8,0"
                    Click="Stop_Click" Style="{StaticResource PrimaryButtonStyle}" />
            <Button Grid.Column="2" Content="Cancel" Width="74" Height="34"
                    Click="Cancel_Click" />
        </Grid>
    </Border>
</Window>
