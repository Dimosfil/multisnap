<Window x:Class="MultiSnap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MultiSnap" Height="760" Width="1120" MinHeight="560" MinWidth="860"
        Background="#111318" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="#181b22" Padding="18">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,22">
                        <Border Width="44" Height="44" Background="#ef1f2d" CornerRadius="8" Margin="0,0,12,0">
                            <TextBlock Text="M" Foreground="White" FontSize="24" FontWeight="Bold"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel>
                            <TextBlock Text="MultiSnap" Foreground="White" FontSize="24" FontWeight="SemiBold" />
                            <TextBlock Text=".NET WPF port" Foreground="#9aa3b2" />
                        </StackPanel>
                    </StackPanel>

                    <Button Content="Capture Area" Height="42" Margin="0,0,0,10" Click="CaptureArea_Click" />
                    <Button Content="Full Screen" Height="42" Margin="0,0,0,22" Click="CaptureFull_Click" />

                    <TextBlock Text="Settings" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <Border BorderBrush="#2a303b" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,22">
                        <StackPanel>
                            <TextBlock Text="Area capture hotkey" Foreground="#cbd3df" />
                            <TextBlock Text="Ctrl+Print Screen" Foreground="#ffffff" FontWeight="SemiBold" Margin="0,4,0,0" />
                            <TextBlock x:Name="HotkeyStatusText" Text="Checking..." Foreground="#ffd166" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="Captured images are copied to clipboard immediately." Foreground="#8d97a8"
                               TextWrapping="Wrap" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Background="#111318">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ToolBarTray Background="#151922">
                <ToolBar Background="#151922">
                    <Button Content="Copy" Click="Copy_Click" />
                    <Button Content="Save PNG" Click="Save_Click" />
                    <Separator />
                    <Button Content="Clear Ink" Click="ClearInk_Click" />
                </ToolBar>
            </ToolBarTray>

            <Border Grid.Row="1" Margin="18" Background="#0c0f14" BorderBrush="#2a303b" BorderThickness="1">
                <Grid x:Name="EditorSurface" ClipToBounds="True">
                    <TextBlock x:Name="EmptyState" Text="Ready to capture" Foreground="#dce3ee"
                               FontSize="30" FontWeight="SemiBold" HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                    <Image x:Name="CaptureImage" Stretch="Uniform" />
                    <InkCanvas x:Name="InkLayer" Background="Transparent" EditingMode="Ink" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
